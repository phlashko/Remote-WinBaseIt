To manually baseline, run baseline.ps1 on target box with an admin powershell prompt.  results saved to c:\temp\<target hostname>

To remotely baseline, add ips to put_ips_here.txt, run Run_me.ps1 from an administrator powershell prompt, enter credentials.   